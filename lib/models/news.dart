class News {
  final int id;
  final String title;
  final DateTime date;
  final String publication;
  final String imagePath;
  final String url;

  News(this.id, this.title, this.date, this.publication, this.imagePath, this.url);
}